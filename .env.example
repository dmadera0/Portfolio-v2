# =============================================================================
# .env.example
# Copy this file to .env and fill in your real values.
# NEVER commit .env to git — it's in .gitignore.
# =============================================================================

# S3 bucket name (must be globally unique — e.g. johndoe-portfolio-2024)
S3_BUCKET=d.madera-porfolio

# AWS region where the S3 bucket lives (e.g. us-east-1, us-west-2)
AWS_REGION=us-east-1

# CloudFront distribution ID (e.g. E3ABC1DEF23456)
# Find this in the AWS Console → CloudFront → Distributions
# or run: aws cloudfront list-distributions --query 'DistributionList.Items[*].[Id,DomainName]' --output table
CLOUDFRONT_DISTRIBUTION_ID=EST245WQ0FXQM
